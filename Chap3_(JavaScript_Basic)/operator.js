console.log("바닐라"+" "+"JS");
let myString = "바닐라";
myString += "JS"; 

let age = 18;
let isAdult = (age >= 18) ? true: false;
//age가 18 이상이면 true, 그렇지 않으면 false를 반환
let point = 92;
let grade = (point >= 90) ? "A": (point >= 80) ? "B" : "C";
//point 가 90이상이면 A 80이상이면 B, 80보다 작으면 C반환